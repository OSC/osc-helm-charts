apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "osc-chat.name" . }}
  labels:
    {{- include "osc-chat.labels" . | nindent 4 }}
imagePullSecrets:
  - name: {{ include "osc.common.imagePullSecret.name" . }}
