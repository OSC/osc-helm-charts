apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: add-nodeselector
spec:
  rules:
    - name: ondemand-nodeselector
      match:
        any:
          - resources:
              kinds:
              - Pod
              namespaces:
              - "user-?*"
      mutate:
        patchStrategicMerge:
          spec:
            nodeSelector:
              osc.edu/role: ondemand
