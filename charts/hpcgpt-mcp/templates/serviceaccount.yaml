apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hpcgpt-mcp.name" . }}
  labels:
    {{- include "hpcgpt-mcp.labels" . | nindent 4 }}
imagePullSecrets:
  - name: {{ include "osc.common.imagePullSecret.name" . }}
